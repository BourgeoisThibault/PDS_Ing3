#swagger: '2.0'
info:
  description: Description d'utilisation de l'API de l'application REST de Esibank.
  version: 1.0.0
  title: API Esibank
  contact:
    name: BOURGEOIS Thibault
    email: thibault.bourgeois@etu.u-pec.fr
host: ws.esibank.inside.esiag.info
basePath: /
tags:
  - name: sample
    description: operation sample
schemes:
- "https"
paths:
  '/sample':
    get:
      tags:
        - sample
      summary: Get all sample
      description: ''
      produces:
        - application/json
      responses:
        '200':
          description: Success
          schema:
            type: array
            items:
              $ref: '#/definitions/Sample'
definitions:
  Sample:
    type: object
    properties:
      id:
        type: integer
        format: int64
        example: 1
      myString:
        type: string
        example: test